#include <stdio.h>
int *array;
void read(int* a,int count){
    for(int i=0;i<count;i++) scanf("%d",&a[i]);
    }
// void dequeue(int *a,){}
void enqueue(){}
void main(){
    int *arr1, *arr2, i,count1, count2;
    printf("Enter no of elements in queue : ");
    scanf("%d",&count1);
    read(arr1,count1);
}